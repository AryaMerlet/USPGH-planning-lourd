<!-- USPGH-planning-lourd/MainWindow.xaml -->
<Window x:Class="USPGH_planning_lourd.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:USPGH_planning_lourd"
        mc:Ignorable="d"
        Title="USPGH User Management" Height="600" Width="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="User Management" 
                  FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <DataGrid x:Name="UsersList" Grid.Row="1" AutoGenerateColumns="False" 
                 ItemsSource="{Binding Users}" IsReadOnly="True" 
                 SelectionMode="Single" Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding first_name}" Width="150"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding last_name}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="250"/>
                <DataGridTextColumn Header="Created" Binding="{Binding created_at, StringFormat={}{0:yyyy-MM-dd}}" Width="100"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="AddUserButton" Content="Add User" Width="100" 
                   Margin="5" Padding="5" Click="AddUser_Click"/>
            <Button x:Name="EditUserButton" Content="Edit User" Width="100" 
                   Margin="5" Padding="5" Click="EditUser_Click"/>
            <Button x:Name="DeleteUserButton" Content="Delete User" Width="100" 
                   Margin="5" Padding="5" Click="DeleteUser_Click"/>
        </StackPanel>
    </Grid>
</Window>